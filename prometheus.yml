global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alert_rules.yml"

scrape_configs:
  - job_name: "kuva-consumer-bff"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["kuva-consumer-bff-service:8080"]
